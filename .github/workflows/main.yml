name: demo
on:

  push:
    branches: ["main"]
    
  pull_request:
    branches: ["main"]
jobs:
  my-job:
    runs-on: ubuntu_latest
    
    steps:
     - name: Run hello world
       run: echo "hello world"
    

  
